<UserControl x:Class="Sati.Views.TeamOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:oxy="http://oxyplot.org/wpf"
             Background="{DynamicResource WindowBackgroundBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24,20,24,32">

            <!-- ── Summary cards ── -->
            <UniformGrid Columns="4"
                         Margin="0,0,0,24">
                <Border Margin="0,0,6,0"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource HoverBrush}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="16,14">
                    <StackPanel>
                        <TextBlock Text="{Binding TotalClients}"
                                   FontFamily="Inter"
                                   FontSize="36"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource TextPrimaryBrush}" />
                        <TextBlock Text="Total Clients"
                                   FontFamily="Inter"
                                   FontSize="11"
                                   Foreground="{DynamicResource TextMutedBrush}"
                                   Margin="0,2,0,0" />
                    </StackPanel>
                </Border>
                <Border Margin="6,0"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource HoverBrush}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="16,14">
                    <StackPanel>
                        <TextBlock Text="{Binding TotalOverdue}"
                                   FontFamily="Inter"
                                   FontSize="36"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource WorkflowPurpleBrush}" />
                        <TextBlock Text="Overdue Items"
                                   FontFamily="Inter"
                                   FontSize="11"
                                   Foreground="{DynamicResource TextMutedBrush}"
                                   Margin="0,2,0,0" />
                    </StackPanel>
                </Border>
                <Border Margin="6,0"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource HoverBrush}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="16,14">
                    <StackPanel>
                        <TextBlock Text="{Binding TotalNotesThisMonth}"
                                   FontFamily="Inter"
                                   FontSize="36"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource TextPrimaryBrush}" />
                        <TextBlock Text="Notes This Month"
                                   FontFamily="Inter"
                                   FontSize="11"
                                   Foreground="{DynamicResource TextMutedBrush}"
                                   Margin="0,2,0,0" />
                    </StackPanel>
                </Border>
                <Border Margin="6,0,0,0"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource HoverBrush}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="16,14">
                    <StackPanel>
                        <TextBlock Text="{Binding AvgComplianceLabel}"
                                   FontFamily="Inter"
                                   FontSize="36"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource CompliantBrush}" />
                        <TextBlock Text="Avg Compliance"
                                   FontFamily="Inter"
                                   FontSize="11"
                                   Foreground="{DynamicResource TextMutedBrush}"
                                   Margin="0,2,0,0" />
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- ── Case manager table ── -->
            <TextBlock Text="CASE MANAGERS"
                       FontFamily="Inter"
                       FontSize="11"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextMutedBrush}"
                       Margin="0,0,0,8" />
            <DataGrid AutomationProperties.Name="Case managers" ItemsSource="{Binding CaseManagers}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="False"
                      CanUserResizeRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      SelectionUnit="FullRow"
                      SelectionMode="Single"
                      Background="Transparent"
                      BorderThickness="0"
                      RowBackground="Transparent"
                      AlternatingRowBackground="{DynamicResource SurfaceRaisedBrush}"
                      HorizontalGridLinesBrush="{DynamicResource HoverBrush}"
                      Margin="0,0,0,28">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background"
                                Value="Transparent" />
                        <Setter Property="Foreground"
                                Value="{DynamicResource TextMutedBrush}" />
                        <Setter Property="FontFamily"
                                Value="Inter" />
                        <Setter Property="FontSize"
                                Value="11" />
                        <Setter Property="FontWeight"
                                Value="SemiBold" />
                        <Setter Property="Padding"
                                Value="8,8" />
                        <Setter Property="BorderThickness"
                                Value="0,0,0,1" />
                        <Setter Property="BorderBrush"
                                Value="{DynamicResource HoverBrush}" />
                        <Setter Property="SeparatorVisibility"
                                Value="Collapsed" />
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness"
                                Value="0" />
                        <Setter Property="Padding"
                                Value="8,6" />
                        <Setter Property="Foreground"
                                Value="{DynamicResource TextPrimaryBrush}" />
                        <Setter Property="FontFamily"
                                Value="Inter" />
                        <Setter Property="Background"
                                Value="Transparent" />
                        <Setter Property="FocusVisualStyle"
                                Value="{x:Null}" />
                        <Style.Triggers>
                            <Trigger Property="IsSelected"
                                     Value="True">
                                <Setter Property="Background"
                                        Value="{DynamicResource HoverBrush}" />
                                <Setter Property="Foreground"
                                        Value="{DynamicResource TextPrimaryBrush}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="BorderThickness"
                                Value="0" />
                        <Style.Triggers>
                            <Trigger Property="IsSelected"
                                     Value="True">
                                <Setter Property="Background"
                                        Value="{DynamicResource HoverBrush}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name"
                                        Binding="{Binding DisplayName}"
                                        Width="*" />
                    <DataGridTextColumn Header="Clients"
                                        Binding="{Binding ClientCount}"
                                        Width="70" />
                    <DataGridTextColumn Header="Overdue"
                                        Binding="{Binding OverdueCount}"
                                        Width="70" />
                    <DataGridTextColumn Header="Notes"
                                        Binding="{Binding NotesThisMonth}"
                                        Width="70" />
                    <DataGridTemplateColumn Header="Progress"
                                            Width="140">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ProgressBar Value="{Binding ProgressPercent, Mode=OneWay}"
                                             Maximum="100"
                                             Height="8"
                                             Margin="8,6"
                                             BorderThickness="0">
                                    <ProgressBar.Style>
                                        <Style TargetType="ProgressBar">
                                            <Setter Property="Foreground"
                                                    Value="{DynamicResource AccentBrush}" />
                                            <Setter Property="Background"
                                                    Value="{DynamicResource HoverBrush}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding StatusLevel}"
                                                             Value="Ok">
                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource CompliantBrush}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding StatusLevel}"
                                                             Value="Danger">
                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource WorkflowPurpleBrush}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ProgressBar.Style>
                                </ProgressBar>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Status"
                                            Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="3"
                                        Padding="6,2"
                                        HorizontalAlignment="Left"
                                        Margin="6,3">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background"
                                                    Value="{DynamicResource HoverBrush}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding StatusLevel}"
                                                             Value="Ok">
                                                    <Setter Property="Background"
                                                            Value="{DynamicResource CompliantFillBrush}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding StatusLevel}"
                                                             Value="Danger">
                                                    <Setter Property="Background"
                                                            Value="{DynamicResource DangerSoftBrush}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding StatusLabel}"
                                               FontFamily="Inter"
                                               FontSize="11"
                                               Foreground="{DynamicResource TextPrimaryBrush}" />
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- ── Compliance chart ── -->
            <TextBlock Text="COMPLIANCE BY CASE MANAGER"
                       FontFamily="Inter"
                       FontSize="11"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextMutedBrush}"
                       Margin="0,0,0,8" />
            <Border Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource HoverBrush}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="12">
                <oxy:PlotView Name="ComplianceChart"
                    Model="{Binding ComplianceChartModel}"
                              Height="300"
                              Background="Transparent" />
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>