{
  "//": "Template for the real appsettings.json, which is gitignored because it holds the connection string. Copy this file to appsettings.json in the project root before the first run. Both the running app (Host.CreateDefaultBuilder) and design-time EF tooling (SatiContextFactory) read it.",

  "ConnectionStrings": {
    "SatiDb": "Server=(localdb)\\MSSQLLocalDB;Database=Sati;Trusted_Connection=True;MultipleActiveResultSets=true;TrustServerCertificate=True"
  },

  "//LocalAi": "Local case-note drafting. Enabled=false removes the feature from the note-entry UI without touching XAML or DI. The first drafting request downloads the ModelAlias variant (multi-gigabyte) into %LOCALAPPDATA%\\Sati\\LocalAi.",

  "LocalAi": {
    "Enabled": false,
    "ModelAlias": "phi-4-mini",
    "MaxInputWords": 500,
    "MaxOutputTokens": 900,
    "RecentContextNoteCount": 10,
    "MatchingContextNoteCount": 5,
    "MaxContextCharacters": 24000,
    "MaxContextNoteCharacters": 1200,
    "MaxAssessmentContextCharacters": 8000,
    "RulesFile": "AI_CASE_NOTE_RULES.md"
  }
}
