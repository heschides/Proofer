<Window x:Class="Sati.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sati.Views"
        Icon="/images/sati.ico"
        mc:Ignorable="d"
        Title="Sati"
        Height="600"
        Width="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}">

    <Window.Resources>
        <Style x:Key="LabelStyle"
               TargetType="TextBlock">
            <Setter Property="FontSize"
                    Value="11" />
            <Setter Property="FontWeight"
                    Value="SemiBold" />
            <Setter Property="Foreground"
                    Value="{DynamicResource TextSecondaryBrush}" />
            <Setter Property="Margin"
                    Value="0,0,0,5" />
        </Style>

        <Style x:Key="InputStyle"
               TargetType="Control">
            <Setter Property="Height"
                    Value="36" />
            <Setter Property="Padding"
                    Value="10,0" />
            <Setter Property="FontSize"
                    Value="14" />
            <Setter Property="Background"
                    Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="BorderBrush"
                    Value="{DynamicResource HoverStrongBrush}" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Setter Property="Foreground"
                    Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="Margin"
                    Value="0,0,0,16" />
        </Style>

        <Style x:Key="PrimaryButton"
               TargetType="Button">
            <Setter Property="Height"
                    Value="40" />
            <Setter Property="Background"
                    Value="{DynamicResource AccentBrush}" />
            <Setter Property="Foreground"
                    Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="FontSize"
                    Value="14" />
            <Setter Property="FontWeight"
                    Value="SemiBold" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="Margin"
                    Value="0,0,0,16" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter Property="Background"
                                        Value="{DynamicResource AccentHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed"
                                     Value="True">
                                <Setter Property="Background"
                                        Value="{DynamicResource AccentPressedBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LinkButton"
               TargetType="Button">
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="Foreground"
                    Value="{DynamicResource AccentBrush}" />
            <Setter Property="FontSize"
                    Value="13" />
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}"
                                   Foreground="{TemplateBinding Foreground}"
                                   TextDecorations="Underline" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter Property="Foreground"
                                        Value="{DynamicResource AccentHoverBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Padding="40">
        <StackPanel VerticalAlignment="Center">

            <!-- Bodhi leaf -->
            <Image Source="pack://application:,,,/images/leaf.png"
                   Height="72"
                   Width="48"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,12"
                   Opacity="0.85" />

            <!-- Title -->
            <TextBlock Text="Sati"
                       FontSize="30"
                       FontWeight="Bold"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       FontFamily="Palatino Linotype"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,4" />
            <TextBlock Text="Case Management"
                       FontSize="12"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,32" />

            <!-- User selector -->
            <TextBlock Text="USERNAME"
                       Style="{StaticResource LabelStyle}" />
            <TextBox AutomationProperties.Name="Username" Style="{StaticResource InputStyle}"
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                     VerticalContentAlignment="Center" />

            <!-- Password -->
            <TextBlock Text="PASSWORD"
                       Style="{StaticResource LabelStyle}" />
            <PasswordBox AutomationProperties.Name="Password" x:Name="PasswordInput"
                         Style="{StaticResource InputStyle}"
                         PasswordChanged="PasswordInput_PasswordChanged"
                         KeyDown="PasswordInput_KeyDown"
                         VerticalContentAlignment="Center" />

            <!-- Sign In -->
            <Button Content="Sign In"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding LoginCommand}" />

            <TextBlock Text="{Binding SignInStatus}"
                       FontSize="11"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       MinHeight="30"
                       Margin="0,-8,0,8" />

            <!-- Divider -->
            <Separator Background="{DynamicResource BorderBrush}"
                       Margin="0,4,0,16" />

            <!-- Create account -->
            <StackPanel Orientation="Horizontal"
                        Visibility="{Binding CanCreateAccount, Converter={StaticResource BoolToVisibilityConverter}}"
                        HorizontalAlignment="Center">
                <TextBlock Text="New to Sati?   "
                           FontSize="13"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           VerticalAlignment="Center" />
                <Button Content="Create an account"
                        Style="{StaticResource LinkButton}"
                        Command="{Binding OpenNewUserWindowCommand}" />
            </StackPanel>

            <Button Content="Forgot password?"
                    Style="{StaticResource LinkButton}"
                    Click="ForgotPassword_Click"
                    HorizontalAlignment="Center"
                    Margin="0,10,0,0"
                    FontSize="10" />

            <!-- Branding -->
            <TextBlock Text="by Robin-Bradley AMS"
                       FontSize="10"
                       Foreground="{DynamicResource InputBorderBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,24,0,0"
                       FontStyle="Italic" />

            <TextBlock Text="{Binding ReleaseVersion}"
                       AutomationProperties.Name="Application version"
                       FontSize="9"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,5,0,0"
                       Opacity="0.7" />

        </StackPanel>
    </Border>
</Window>
